/*

"use strict";

const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 300,
    disponible: true,
}

Object.seal( producto );
producto.disponible = false;



console.log(producto);

console.log(Object.isSealed.producto);

//Diferencia entre Frozen y Sealed --> Frozen no te permite tocar nada en el objeto. Sealed te permite modificar propiedas pero no agregar ni borrar

*/

